<div class="container">
    <div class="row">
        <div class="col-md-8 mx-auto post-form-container">
            <h2 class="h2"><%= @user_profile.name %></h2>
            <!-- user_profileテーブルのnameカラムを取り出してる-->
            <% if current_user != @user %>
                <% if current_user.following?(user) %>
                    <%= link_to "フォロー外す", destroy_profile_path(@follow), method: :delete,, data: { confirm: '本当に削除しますか？' }, class: "btn btn-danger" %>
                <% else %>
                    <%= link_to "フォローする", follow_profile_path(@user_profile.user_id),　 local: true, method: :post, class:"btn btn-success" %>
                <% end %>
            
            
                   <!-- <%= form_with(url: follow_profile_path(@user_profile.user_id), local: true, method: :post, html: { class: 'post-form' }) do |f| %>
                        <div class="button-group">                    
                            <%= f.submit 'フォローする', class: 'btn btn-primary' %>
                        </div>

                        <%= form_with(url: destroy_profile_path(@follow), method: :delete, data: { confirm: '本当に削除しますか？' }, class: "btn btn-danger" %>
                            <div class="button-group">
                                <%= f.submit 'フォローをやめる', class: 'btn btn-primary' %>
                            </div>
                        <%end%> 
                    <%end%> -->
        </div>
    </div>
</div>